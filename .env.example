# Optional. Public discovery works without secrets, but GitHub Search has a
# higher rate limit when this token is supplied by GitHub Actions.
GITHUB_TOKEN=

# Optional GitHub Pages or custom-domain overrides.
PUBLIC_SITE_URL=
PUBLIC_BASE_PATH=
